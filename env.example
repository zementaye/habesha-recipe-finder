# Copy this file to .env and fill in your own values.

# Optional: restrict CORS to your deployed frontend's origin in production.
# FRONTEND_URL=https://your-app.example.com

# Optional: requests/minute/IP allowed against the API (default 60).
# RATE_LIMIT_MAX=60
